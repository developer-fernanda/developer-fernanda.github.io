import { makeStyles } from '@material-ui/core/styles';
const formSettingsStyles = makeStyles(theme => {
	return {
		paper: {
			marginTop: theme.spacing(2),
			padding: theme.spacing(3),
		}
	}
});

export default formSettingsStyles
